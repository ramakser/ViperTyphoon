{% include 'header' %}

@protocol {{ prefix }}{{ module_info.name }}ViewInterface;
@protocol {{ prefix }}{{ module_info.name }}PresenterInterface;

@interface {{ prefix }}{{ module_info.name }}Router : NSObject

@property (nonatomic, weak) id<{{ prefix }}{{ module_info.name }}ViewInterface> view;
@property (nonatomic, weak) id<{{ prefix }}{{ module_info.name }}PresenterInterface> presenter;

@end
