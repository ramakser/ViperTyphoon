{% include 'header' %}

#import <UIKit/UIKit.h>

#import "{{ prefix }}{{ module_info.name }}ViewInterface.h"

@protocol {{ prefix }}{{ module_info.name }}PresenterInterface;

@interface {{ prefix }}{{ module_info.name }}ViewController : UIViewController <{{ prefix }}{{ module_info.name }}ViewInterface>

@property (nonatomic, strong) id<{{ prefix }}{{ module_info.name }}PresenterInterface> presenter;

@end
